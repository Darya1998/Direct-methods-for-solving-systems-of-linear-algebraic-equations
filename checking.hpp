#ifndef _CHECKING_
#define _CHECKING_

#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <sstream>
#include <iomanip>
#include <math.h>

template <typename Type>
std::vector<std::vector<Type>> checking(std::vector<std::vector<Type>> q_matrix);

template <typename Type>
std::vector<std::vector<Type>> checking(std::vector<std::vector<Type>> matrix){

}


using namespace std;

#endif